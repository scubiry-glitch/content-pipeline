# v1.1 测试用例

**版本**: v1.1
**测试日期**: 2026-03-17
**测试范围**: TaskDetail 类型修复与功能增强

---

## 测试用例清单

### TC-001: 类型定义完整性

**目的**: 验证 TypeScript 类型定义与 API 返回数据一致

**测试步骤**:
1. 编译项目: `npm run build`
2. 检查类型错误

**预期结果**:
- [x] 无 TypeScript 编译错误
- [x] `Task.writing_data` 字段可访问
- [x] `Task.asset_ids` 字段可访问
- [x] `NovelAngle.potentialImpact` 字段可访问

**实际结果**: 编译通过

---

### TC-002: 大纲确认按钮显示逻辑

**目的**: 验证大纲确认按钮在正确状态下显示

**测试步骤**:
1. 创建新任务，状态为 `planning`
2. 打开任务详情页
3. 切换到"选题策划"标签
4. 观察"确认大纲并继续"按钮

**预期结果**:
- [x] 状态为 `planning` 时按钮显示
- [x] 状态为 `outline_pending` 时按钮显示（兼容旧数据）
- [x] 其他状态下按钮不显示

**实际结果**: 待验证

---

### TC-003: 快捷操作按钮加载状态

**目的**: 验证按钮加载状态防止重复点击

**测试步骤**:
1. 打开任务详情页
2. 点击"重做选题策划"按钮
3. 观察按钮状态变化
4. 等待操作完成

**预期结果**:
- [x] 点击后按钮显示"重算中..."
- [x] 按钮处于禁用状态
- [x] 操作完成后恢复原状
- [x] 网络错误时恢复按钮状态

**实际结果**: 待验证

---

### TC-004: 热点话题渲染

**目的**: 验证热点话题字段名正确匹配

**测试步骤**:
1. 打开任务详情页
2. 切换到"质量分析"标签
3. 观察热点话题列表

**预期结果**:
- [x] 热点话题标题显示正确（使用 `title` 字段）
- [x] 热度值显示正确（使用 `hotScore` 字段）
- [x] 空数据时显示"暂无热点数据"

**实际结果**: 待验证

---

### TC-005: 情感分析分布计算

**目的**: 验证情感分析分布计算避免除以零

**测试步骤**:
1. 打开任务详情页
2. 切换到"质量分析"标签
3. 观察情感分析分布条

**测试数据**:
- 场景A: positive=10, neutral=5, negative=3
- 场景B: positive=0, neutral=0, negative=0

**预期结果**:
- [x] 场景A: 分布条按比例显示
- [x] 场景B: 分布条显示为0%，无NaN错误

**实际结果**: 待验证

---

### TC-006: NovelAngle 字段兼容

**目的**: 验证 NovelAngle 字段向后兼容

**测试步骤**:
1. 打开一个有知识库洞见的任务
2. 切换到"选题策划"标签
3. 观察"建议的新研究角度"区域

**测试数据**:
```json
{
  "angle": "政策风险角度",
  "description": "从政策不确定性角度分析",
  "differentiation_score": 8,
  "potentialImpact": "high"
}
```

**预期结果**:
- [x] 角度标题显示正确
- [x] 影响力标签显示正确（高/中/低）
- [x] 差异化评分显示正确

**实际结果**: 待验证

---

### TC-007: handleRedoStage 通用函数

**目的**: 验证重做阶段通用函数工作正常

**测试步骤**:
1. 打开任务详情页
2. 点击"重做深度研究"按钮
3. 确认弹窗
4. 观察请求发送

**预期结果**:
- [x] 弹窗确认后发送 `POST /production/:id/redo/research`
- [x] 加载状态正确显示
- [x] 成功后刷新任务数据
- [x] 失败后显示错误提示

**实际结果**: 待验证

---

## 回归测试

### TC-008: 现有功能未受影响

**测试范围**:
- [x] 任务列表加载
- [x] 任务详情加载
- [x] 蓝军评审展示
- [x] 大纲编辑保存
- [x] 素材关联

**实际结果**: 待验证

---

## 测试结论

| 项目 | 结果 |
|------|------|
| 类型定义 | 通过 |
| 按钮显示逻辑 | 待验证 |
| 加载状态 | 待验证 |
| 数据渲染 | 待验证 |
| 边界情况 | 待验证 |

---

**测试人**: 架构师
**下次测试**: v1.2 开发完成后
